{
  "name": "oab-sim-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter @oab/api prisma:migrate",
    "db:seed": "pnpm --filter @oab/api prisma:seed"
  }
}
